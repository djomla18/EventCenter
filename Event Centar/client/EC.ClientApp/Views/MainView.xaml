<Window x:Name="SearchView" x:Class="EC.ClientApp.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EC.ClientApp.Views"
        mc:Ignorable="d"
        Height="1080" 
        Width="1920"
        WindowState="Maximized"
        WindowStyle="None"
        Background="#F5F5DC">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="150"/> <!-- search bar-->
            <RowDefinition Height="*"/> <!-- rezultati pretrage -->
            <RowDefinition Height="350"/> <!-- tastatura -->
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Padding="40"
                Margin="20,10,20,0"
                CornerRadius="20"
                Background="#FDFBF7">
            <TextBlock Text="Search Screen - Soon"
                       FontSize="40"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Top"/>        
        </Border>

        <Border Grid.Row="1"
                Margin="20,10,20,0"
                Background="#FDFBF7"
                CornerRadius="20">
            <TextBlock Text="Rezultati pretrage"
                       FontSize="40"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"/>
        </Border>

        <Border Grid.Row="2"
                Background="#D4CFC0"
                Margin="20,10,20,20"
                CornerRadius="20">
            <TextBlock Text="Rezultati pretrage"
                       FontSize="40"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"/>
        </Border>

    </Grid>
</Window>
